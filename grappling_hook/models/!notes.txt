The files in this folder are just defining interface that the rest of the lua script conforms to

Lots of functionality will be written around these types

So using c#, because it's easier to define types in that (also, the files won't be seen as lua
and try to be interpreted at runtime)